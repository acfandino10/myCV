# TODO

- [x] Update package.json to replace `node-sass` with `sass` for modern Node compatibility
- [x] Run `npm install` to refresh dependencies and lockfile
- [ ] Run project with `npm run serve` and confirm it starts
